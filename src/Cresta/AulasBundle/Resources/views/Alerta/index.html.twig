{% extends '::base.html.twig' %}
{% block title %}Alertas {% endblock %}
{% block body -%}
    <h1>Lista de Alertas</h1>
    <ul>
            <a href="{{ path('aulas_alerta_new') }}" class="btn btn-default botonTabla">
                Nuevo alerta
            </a>
    </ul>
    <table class="records_list tabla">
        <thead>
            <tr>
                <th>Fecha</th>
                <th>Descripción</th>
                <th>Observaciones</th>
                <th>Acción</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{% if entity.fecha %}{{ entity.fecha|date('d/m/Y') }}{% endif %}</td>
                <td>{{ entity.descripcion }}</td>
                <td>{{ entity.observaciones }}</td>
                <td>
                <ul>
                    
                        <a href="{{ path('aulas_alerta_show', { 'id': entity.id }) }}"><img src="{{ asset('img/iconos/ver.png') }}" class="icono" alt="Ver" /></a>
                    
                    
                        <a href="{{ path('aulas_alerta_edit', { 'id': entity.id }) }}"><img src="{{ asset('img/iconos/editar.png') }}" class="icono" alt="Editar" /></a>

                        <a href="{{ path('aulas_alerta_delete', { 'id': entity.id }) }}"><img src="{{ asset('img/iconos/borrar.png') }}" class="icono" alt="Borrar" /></a>
                    
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
<<<<<<< HEAD

        <ul>
        <li>
            <a href="{{ path('aulas_alerta_new') }}">
                Create a new entry
            </a>
            <form name="pruebaPDF" action="{{ path('pdf_file', { name: 'valor' }) }}" method="GET">
                <input type="button" action="submit" value="name" name="submit">
            </form> 
        </li>
    </ul>
=======
>>>>>>> d6a303619925c21acf15c39f228ad2d5a11c46ad
    {% endblock %}
