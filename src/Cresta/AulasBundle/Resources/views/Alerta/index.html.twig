{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Alerta list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fecha</th>
                <th>Descripcion</th>
                <th>Observaciones</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('aulas_alerta_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.fecha }}</td>
                <td>{{ entity.descripcion }}</td>
                <td>{{ entity.observaciones }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('aulas_alerta_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('aulas_alerta_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('aulas_alerta_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}