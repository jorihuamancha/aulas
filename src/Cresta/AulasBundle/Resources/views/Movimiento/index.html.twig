{% extends '::base.html.twig' %}
{% block title %}Movimientos {% endblock %}
{% block body -%}
    <h1>Lista de Movimientos</h1>

    <table class="records_list tabla">
        <thead>
            <tr>
                <th>Fecha</th>
                <th>Usuario</th>
                <th>Aula</th>
                <th>Fecha desde</th>
                <th>Fecha hasta</th>

            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.fecha }}</td>
                <td>{{ reservaEliminada.usuario }}</td>
                <td>{{ reservaEliminada.aula }}</td>
                <td>{{ reservaEliminada.fechaDesde |date('H:i:s') }}</td>
                <td>{{ reservaEliminada.fechaHasta |date('H:i:s') }}</td>                

                        <a href="{{ path('aulas_movimiento_show', { 'id': entity.id }) }}"><img src="{{ asset('img/iconos/ver.png') }}" class="icono" alt="Ver" /></a>                        
                        
                        
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{#    <ul>
            <a href="#top" class="btn btn-default botonTabla">
                Volver arriba
            </a>
    </ul>
#}
    {% endblock %}
